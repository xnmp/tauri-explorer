# Bookmark file-drop destinations

Specific bookmark rows must be resolved before their surrounding Bookmarks
section. A row is a transfer destination; the section's empty space remains
the deliberate folder-pinning surface. This ordering is shared by Linux native
drop hit-testing and the pointer-drag path used by the other supported engines.
