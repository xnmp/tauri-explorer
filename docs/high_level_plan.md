* Bare bones demo
    * a basic frontend in Svelte
    * ~~just use a fastapi backend with an endpoint that lists all the files in a directory~~ (Migrated to Rust)
    * read only, no write operations
* add some basic write operations
* create a rust backend ✅ Complete (Issue: tauri-explorer-nv2y)
* Draft architecture for MVP

