name: Tauri binary E2E (smoke)

on:
  pull_request:
    branches: [dev, main]
  push:
    branches: [dev, main]
  workflow_dispatch:

# Note: macOS is deliberately omitted. Official `tauri-driver` has no
# WKWebView support (tauri-apps/tauri#7068). Re-add when a stable driver
# exists or if macOS regressions justify the CrabNebula subscription.
jobs:
  smoke:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    # The Windows debug build alone is ~12 minutes, so a 25-minute job cap left
    # no room for a full suite run: a step bound generous enough to let the
    # specs finish pushed the job past the cap and it was killed as `cancelled`
    # with no artifacts. Cap the job well above build + suite and let the
    # per-step bound below be the thing that actually fails a stuck run.
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - uses: dtolnay/rust-toolchain@stable

      # Registry/git only — NOT the tauri-driver binary. Caching the binary
      # under a Cargo.lock-derived key let a run where `cargo install` failed
      # save a binary-less cache, which later runs then restored (ENOENT at
      # spawn). tauri-driver is now installed unconditionally below.
      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-registry-

      - name: Cache Rust target
        uses: actions/cache@v6
        with:
          path: src-tauri/target
          key: ${{ runner.os }}-target-debug-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-target-debug-

      # nasm: required by turbojpeg-sys (x86 SIMD) since the thumbnail cache
      # moved to turbojpeg. Without it the cargo build fails in cmake.
      # (AVIF/dav1d is a default-off feature, so no libdav1d-dev needed here.)
      - name: Install Linux WebKit driver
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev libsoup-3.0-dev \
            libjavascriptcoregtk-4.1-dev webkit2gtk-driver xvfb nasm

      # Install unconditionally and fail loudly: a missing binary surfaces
      # here, not as an opaque ENOENT when wdio spawns it. Verify it lands.
      - name: Install tauri-driver
        shell: bash
        run: |
          cargo install tauri-driver --locked
          ls -l "$HOME/.cargo/bin/tauri-driver"*

      # bun.lock (text format) records optional deps for every platform,
      # so the same lockfile installs correctly on Linux and Windows.
      - name: Install frontend dependencies
        shell: bash
        run: bun install --frozen-lockfile

      # Build through the Tauri CLI, not `cargo build`. A bare cargo debug
      # build omits the `tauri/custom-protocol` feature, so the binary serves
      # `build.devUrl` (localhost:1420) and the suite needs a Vite dev server
      # alongside it. That coupling is what broke Windows (#457): the webview's
      # first paint blocks on a cold dev server transforming the whole module
      # graph, msedgedriver's new-session waits for that load, and once the
      # graph outgrew the 60s connectionRetryTimeout the handshake never
      # returned — the session POST died with hyper IncompleteMessage on every
      # run. `tauri build --debug` passes custom-protocol, so the frontend is
      # embedded in the binary and there is no dev server to race.
      # VITE_E2E_HOOKS compiles in the `e2e-navigate` / `e2e-reset-view` hooks
      # and the listing/watcher probes the specs drive. They used to ride on
      # `import.meta.env.DEV`, which is what tied the suite to a dev-mode
      # binary — and therefore to a dev server — in the first place. Release
      # builds never set it, so the hooks tree-shake out of shipped binaries.
      # VITE_E2E_NO_WARM_PRIME (Windows only): msedgedriver's "launch" approach
      # attaches to the FIRST WebView2 instance an app creates, and Microsoft
      # documents multi-instance apps as requiring the "attach" approach. This
      # app parks a hidden warm window ~1.5s after boot (warmWindow defaults
      # on), so a second WebView2 lands mid-attach and POST /session never
      # returns. WebKitGTK tolerates it, so Linux keeps warm-window coverage.
      - name: Build Tauri binary (debug, embedded frontend, no bundle)
        shell: bash
        env:
          VITE_E2E_HOOKS: "1"
          VITE_E2E_NO_WARM_PRIME: ${{ runner.os == 'Windows' && '1' || '' }}
        run: bun run tauri build --debug --no-bundle

      - name: Run smoke suite (Linux, under Xvfb)
        if: runner.os == 'Linux'
        run: xvfb-run -a --server-args="-screen 0 1280x1024x24" bun run test:e2e:tauri

      # The runner's EDGEWEBDRIVER matches the Edge *browser*, but tauri apps
      # run on the WebView2 *runtime*, whose version can differ — msedgedriver
      # hard-rejects mismatches. Download the driver matching the runtime.
      - name: Install msedgedriver matching the WebView2 runtime
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $key = 'HKLM:\SOFTWARE\WOW6432Node\Microsoft\EdgeUpdate\Clients\{F3017226-FE2A-4295-8BDF-00C3A9A7E4C5}'
          $version = (Get-ItemProperty $key).pv
          Write-Host "WebView2 runtime version: $version"
          Invoke-WebRequest "https://msedgedriver.microsoft.com/$version/edgedriver_win64.zip" -OutFile edgedriver.zip
          Expand-Archive edgedriver.zip -DestinationPath "$env:RUNNER_TEMP\edgedriver"
          "TAURI_NATIVE_DRIVER=$env:RUNNER_TEMP\edgedriver\msedgedriver.exe" >> $env:GITHUB_ENV

      # Bounded well below the 25-minute job cap so a regression leaves time for
      # the artifact upload below instead of being killed by the cap with
      # nothing to show. Sized for a real run, not for the fast-fail path: the
      # Linux leg's whole job is ~10 minutes, so anything near the ~1 minute a
      # session-start failure takes would kill a working suite.
      - name: Run smoke suite (Windows)
        if: runner.os == 'Windows'
        timeout-minutes: 15
        env:
          # Must match the build flag above — wdio uses it to exclude
          # warm-window.spec, which asserts the very window it suppresses.
          VITE_E2E_NO_WARM_PRIME: "1"
        run: bun run test:e2e:tauri

      - name: Upload WDIO logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: wdio-logs-${{ matrix.os }}
          path: |
            logs/
            e2e-tauri/logs/
          if-no-files-found: ignore
